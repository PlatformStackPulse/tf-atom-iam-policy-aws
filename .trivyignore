# IAM Policy Atom — single-resource module
# Companion resources (role, user, group attachments) are separate atoms
AVD-AWS-0057  # IAM policy wildcard check — context-dependent, validated externally
